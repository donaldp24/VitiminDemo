package com.general.mediaplayer.VitiminDemo;

import android.view.View;
import android.widget.ImageView;
import android.widget.RelativeLayout;

/**
 * Created by donald on 4/4/14.
 */
public class ResultArranger {
    public void arrangeResult(View viewParent, ResultInfo resultInfo)
    {
        /*
        // Set Icon
        ImageView imgIcon = (ImageView)(viewParent.findViewById(R.id.img_resulticon));
        imgIcon.setBackgroundResource(resultInfo.idIcon);
        RelativeLayout.MarginLayoutParams iconParams = (RelativeLayout.LayoutParams)imgIcon.getLayoutParams();

        iconParams.leftMargin = resultInfo.rtIcon.left;
        iconParams.topMargin = resultInfo.rtIcon.top;
        iconParams.width = resultInfo.rtIcon.right;
        iconParams.height = resultInfo.rtIcon.bottom;
        imgIcon.setLayoutParams(iconParams);

        // Set Text
        ImageView imgText = (ImageView)(viewParent.findViewById(R.id.img_resulttext));
        imgText.setBackgroundResource(resultInfo.idText);
        RelativeLayout.MarginLayoutParams textParams = (RelativeLayout.LayoutParams)imgText.getLayoutParams();
        textParams.leftMargin = resultInfo.rtText.left;
        textParams.topMargin = resultInfo.rtText.top;
        textParams.width = resultInfo.rtText.right;
        textParams.height = resultInfo.rtText.bottom;
        imgText.setLayoutParams(textParams);
        */
    }
}
